coordinator:
  host: "0.0.0.0"
  port: 8090
  suspect_timeout: 10s
  dead_timeout: 20s
  heartbeat_interval: 5s
  dispatch_interval: 1s
  vnodes_per_node: 150
  task_max_retries: 3

metrics:
  enabled: true
  port: 9090
  path: /metrics

logging:
  level: info
  format: text
